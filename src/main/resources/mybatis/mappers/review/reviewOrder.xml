<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yogiyo.review.dao.ReviewOrderDao">

	<!-- userNo로 주문내역 조회하기 -->
	<select id="getOrderInfoByUserNo" parameterType="string" resultType="com.yogiyo.review.vo.ReviewOrderItem">
		select
			b.order_item_amount, b.order_item_option_menu_names 
	 	from	
	 		 yogiyo_orders a, yogiyo_order_items b
	 	where
	 		 a.order_no = b.order_no
	 	and
			 user_no = #{value}
	</select>

</mapper>